# 複数同時研究のGit運用

このリポジトリでは、製品実装と機材・UI・音響の研究を同時に進める。研究同士を一つの作業ブランチや一つの共有メモへ混ぜない。

## 研究単位

研究ごとに一意の `research-id` を付ける。

```
YYYYMMDD-短い英数字名
```

例: `20260828-loopy-pro`、`20260828-rc505mk2`、`20260828-kaoss-master-fx`

各研究は次の組で分離する。

- ブランチ: `research/<research-id>`
- 記録: `research/<research-id>/README.md`
- 必要なら同じディレクトリ配下に比較表、画像、試作を置く
- PR: 一研究につき一件。別研究の変更を混ぜない

## 開始

1. `main` の最新コミットから `research/<research-id>` を作る。
2. `research/<research-id>/README.md` を作り、目的、観測、推論、設計への採否を分けて書く。
3. 実装を触る場合は、READMEに対象パスを列挙する。
4. 同じ対象パスを触る既存PRまたは現役の修理ロックがあれば、そのパスの変更は止める。研究記録自体は独立して続けてよい。

## 研究記録の最低項目

- `research-id`
- 研究対象
- 現在の問い
- 一次資料または実機観測
- 観測できた事実
- 推論・仮説
- Field Looperへ採用する点
- 採用しない点
- 触る実装パス
- 依存する研究
- 未検証事項
- 更新日時

事実、ユーザーの実機評価、こちらの推論を同じ欄へ混ぜない。後の訂正は古い記述を黙って消さず、どの判断が失効したかを残す。

## 統合

- 研究PRは `main` の最新状態へ追従してから統合する。
- 研究記録だけのPRは、対象ディレクトリが固有なら並行統合できる。
- 製品コードを含むPRは、変更パスが重ならない場合だけ並行統合できる。
- 同じ製品コードを触る場合、後発研究は先行PRの統合後にrebaseし、実機評価を再実施する。
- 複数研究から一つの製品機能を作る場合、各研究PRを先に保存し、その後 `integration/<機能名>` ブランチで統合する。研究ブランチ同士を直接mergeしない。
- `main` へ未検証の試作を直接入れない。

## 現在の研究候補

Loopy Pro、RC-505mkII、KAOSS PAD / KAOSSILATOR、Strymon、Dedalus、OP-1 field、グリッチ、ドリルンベース、ダブなどは、それぞれ別の `research-id` として扱う。最終的にField Looperへ採る内容だけを統合ブランチへ移す。
