interface:
  display_name: "Sound Lab研究ルーター"
  short_description: "音楽アプリ研究をGit正本へ接続し、証拠と採否を分離する"
  default_prompt: "Use $sound-lab-research-router to resolve the latest sound-lab main SHA, read research/CURRENT.json and the matching record at that SHA, then continue this task from current Git evidence."

policy:
  allow_implicit_invocation: true
